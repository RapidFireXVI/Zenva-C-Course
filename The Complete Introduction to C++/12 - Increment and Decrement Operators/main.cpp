/*

#include <iostream>

int main() {
  // ++ --
  int age = 28;
  // age = age + 1;
  // age += 1;
  // age++;
  // std::cout << age << std::endl;
  // age--;
  // std::cout << age << std::endl;

  int newAge1 = age++;
  age = 28;
  int newAge2 = ++age;
  // std::cout << newAge1 << std::endl;
  // std::cout << newAge2 << std::endl;

  // 1. What is the end result? 0
  int a = 0;
  // int result = a - 5 * 2 + 20 / 2;

  // 2. Can we change the equation to get a positive value?
  int result = (a - 5 * 2 + 20) / 2;
  // std::cout << result << std::endl;

  // 3. What is the end result? 24
  int b = 10;
  b += 2 * 7;
  // std::cout << b << std::endl;

  // 4. I want to spell my full name. What is wrong with this equation?
  std::string firstName = "Nimish ";
  std::string lastName = "Narang";
  std::string fullName = firstName += lastName;
  // std::cout << fullName << std::endl;

  // 5. What are the values of c and d? c = 0 d = 1
  int c = 1;
  int d = c--;
  std::cout << c << std::endl;
  std::cout << d << std::endl;

  }

*/